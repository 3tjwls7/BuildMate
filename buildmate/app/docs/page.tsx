import {AppShell} from "@/components/layout/app-shell";import {Card,Heading} from "@/components/ui/primitives";import { playfulFont } from "@/lib/playful-font";export default function Page(){return <AppShell><Heading eyebrow="DOCUMENTATION" title="아이디어에서 API 명세까지">BuildMate는 결정을 돕고, 최종 조건은 사용자가 정합니다.</Heading><div className="space-y-4">{[["01 · 기능 입력","구현하려는 기능을 자연어로 입력합니다."],["02 · 고려사항 선택","권한, 예외, 상태 전이, 동시성을 검토하고 필요한 조건과 옵션을 선택합니다."],["03 · 명세 확인","확정한 요구사항을 바탕으로 명세를 확인합니다. 현재는 쿠폰 샘플만 제공합니다."]].map(([title,text])=><Card key={title}><h2 className={playfulFont.className + " text-2xl"}>{title}</h2><p className="mt-3 text-sm text-[#73788b]">{text}</p></Card>)}</div></AppShell>;}

